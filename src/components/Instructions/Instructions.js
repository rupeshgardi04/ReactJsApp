import React, { Component } from "react";
import './Instructions.css';

export default class Instruction extends Component {
    render() {
        return (
            <p className="instruction-txt">Click on emoji to know the name</p>
        )
    }
}